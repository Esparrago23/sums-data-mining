# ============================================================================
# SUMS · Módulo de Minería de Datos — dependencias UNIFICADAS (Subcomp. A, B y C)
# ----------------------------------------------------------------------------
# Probado con Python 3.13.7 en el venv: C:\Users\minis\.venvs\sums-mineria
# Crear el entorno e instalar:
#     python -m venv C:\Users\minis\.venvs\sums-mineria
#     C:\Users\minis\.venvs\sums-mineria\Scripts\python.exe -m pip install -r requirements.txt
#     C:\Users\minis\.venvs\sums-mineria\Scripts\python.exe -m spacy download es_core_news_sm
# (el modelo de spaCy NO se instala por nombre desde PyPI; se baja con el comando de arriba)
# Requisito del sistema para Subcomp. A:
#     Ubuntu/Debian: sudo apt install poppler-utils   # provee pdftoppm
#     Windows: instalar Poppler y agregar bin/ al PATH
# ============================================================================

# --- OCR estructurado / visión (Subcomponente A) --- // De los requiriments.txt originales
# opencv-python          # cv2: binarización, contornos, HoughLines, overlays // Esto solo funciona en una PC local
# pymupdf                # fallback puro Python para renderizar PDFs si Poppler no esta disponible

# --- OCR estructurado / visión (Subcomponente A) --- // Arreglados por la IA para el Deploy
opencv-python-headless>=4.9.0  # cv2: binarización, contornos, HoughLines, overlays
pymupdf>=1.24.0         # fallback puro Python para renderizar PDFs si Poppler no esta disponible
pytesseract>=0.3.10     # usado por subcomponente_A_OCR/src/text_trainer.py

# --- Datos / ML (Subcomponente B) ---
numpy
pandas>=2.2            # probado con 3.0.3 — usar la API sin `inplace` en column-slices
scikit-learn>=1.5      # probado con 1.9.0
xgboost>=2.0           # probado con 3.3.0

# --- NLP / IR (Subcomponente C) ---
spacy>=3.8,<3.9        # probado con 3.8.14  (+ modelo es_core_news_sm vía spacy download)
nltk>=3.8.1            # SnowballStemmer para la comparación stemming-vs-lematización (3.8.1 corrige CVE-2024-39705 al añadir --unsafe / sandboxing en downloader)

# --- Visualización y entregable académico (notebooks) ---
matplotlib>=3.8.0
seaborn>=0.13.0
jupyter>=1.0.0
nbconvert>=7.16.4       # fija CVE-2024-* de plantillas/XSS en export HTML
ipykernel>=6.29.0

# --- Integración (API de ejemplo: integracion/api_mineria.py) ---
<<<<<<< HEAD
fastapi                # probado con 0.138.1
uvicorn[standard]      # probado con 0.49.0  (servidor ASGI)
httpx                  # cliente para pruebas con TestClient
python-multipart       # para procesar archivos en la API
