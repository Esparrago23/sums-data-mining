# ─── Fase B-1: Generación de datos sintéticos ───────────────────────────────
# El generador (synthetic_generator.py) usa SOLO la librería estándar de Python.
# No requiere nada para generar las familias.

# ─── Carga a la BD ──────────────────────────────────────────────────────────
requests>=2.31         # load_to_bd.py  (vía API captura-completa) — RECOMENDADO
psycopg2-binary>=2.9   # load_to_bd_sql.py (carga directa a PostgreSQL) — respaldo

# ─── Fases B-2 a B-6: ETL + modelo de riesgo (módulos src/ + notebook) ──────
# Versiones probadas en el venv `sums-mineria` (jun-2026).
# NOTA pandas 3.0: el ETL evita `fillna(..., inplace=True)` (roto en 3.0) y
# fuerza los booleanos a bool real. XGBoost 3.3 requiere LabelEncoder del label.
pandas>=2.0           # probado con 3.0.3
numpy>=1.24
scikit-learn>=1.3     # probado con 1.9.0 (Pipeline + ColumnTransformer)
xgboost>=2.0          # probado con 3.3.0
seaborn>=0.12
matplotlib>=3.7
jupyter>=1.0
nbconvert>=7.0        # ejecución headless del notebook (run_all del notebook)
nbformat>=5.0
ipykernel>=6.0

# Opcional — escalado estadístico adicional del dataset (no obligatorio):
# sdv>=1.9
